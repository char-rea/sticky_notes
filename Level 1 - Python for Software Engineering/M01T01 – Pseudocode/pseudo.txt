pseudo.txt


1. 
set "user_number" to one

set "numbers" to zero

while "user_number" is not equal to zero 

	request integer from the user

	if the integer is not zero 

		store input into variable called "numbers"

	else the integer is a zero value

		print the largest integer that was entered in “numbers”



2. 

request user’s name

store input into variable called “first_name”

print first_name, “Hello, World”



3.

request user to input a random number of integers

store count of random numbers in variable named “counter”
store sum of random numbers in variable named "sum"

divide "sum" by "counter" 

store output as "average"

print "average"


4. 

request user to input a random grocery list and the products status

if product status is available 
	store output into variable named “grocery_list” in alphabetical order

print "grocery_list" 


5.

start

read X, Y

set "S" to the value of X plus Y
set "A" to the value of S divided by 2
set "P" to the value of X times Y
	
print S, A, P

stop

# I used the PDF and also internet to understand some aspects of pseudocode and loops and what is required as well as seeking help from my friend Chelsey and a mentor session